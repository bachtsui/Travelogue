<h1><%= @user.first_name %>'s profile</h1>

<div class = "container">
  <h3><%= @user.first_name %> <%= @user.last_name %></h3>
  <p><strong>E-mail</strong> <%= @user.email %></p>
  <p><strong>Current City</strong> <%= @user.current_city %></p>
  <p><strong>Join Date</strong> <%= @user.created_at.strftime("%B %-d, %Y, at %l:%M%P") %></p>
  <p><img class="photo" src = "<%= @user.profile_photo %>" </p>
  <div class = "buttons">
    <%= link_to "Edit", edit_user_path(@user.id) %>
    <%= button_to "Delete", @user, method: :delete %>
  </div>
</div>

<div class = "container">
	<ul>
	  <strong><% @user.articles.each do |article| %>
      <%= link_to article.title, article_path(article.id) %></strong>
    <div class = "button">
	    <%= link_to "Edit", edit_article_path(article.id) %>
	    <%= button_to "Delete", article, method: :delete %>
    </div>
	  <% end %>
	</ul>
</div>
